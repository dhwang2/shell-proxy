{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'for f in $CLAUDE_FILE_PATHS; do case \"$f\" in *.sh) bash -n \"$f\" 2>&1 || echo \"SYNTAX ERROR in $f\" ;; esac; done'",
            "description": "Auto-validate shell script syntax after edit"
          }
        ]
      }
    ]
  }
}
